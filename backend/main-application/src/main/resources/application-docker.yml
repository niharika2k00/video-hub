server:
  port: 4040

spring:
  application:
    name: Spring Boot Java Project
  main:
    banner-mode: off
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  datasource:
    url: "jdbc:mysql://mysql:3306/videohub"
    username: "root"
    password: "pwroot"
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
  kafka:
    bootstrap-servers: 3.128.201.123:9093 # EC2-B's public IP and external kafka port

logging:
  level:
    root: warn

# Custom environment variables
custom:
  storage-type: "AWS_S3"
  endpoints:
    server:
      local: "http://localhost:4040"
      staging: "http://18.219.151.181:4040"
      # production: "http://18.219.151.181:4040"
    client:
      local: "http://localhost:5173" # frontend base endpoint
      # staging: "http://18.219.151.181:5173"
      # production: "http://18.219.151.181:4040"
  path:
    project-root-dir: ${CUSTOM_PATH_PROJECT_ROOT_DIR:/app}
    source-video-dir: ${CUSTOM_PATH_SOURCE_VIDEO_DIR:/app/videos}
aws:
  credentials:
    profile: "default"
    region: "us-east-1"
  s3:
    bucket-name: "demobucket-890291224"
